# live-race 要件定義書

ブラウザで動く「レース型抽選ツール」。Zoom等の定例会議・ウェビナーで主催者が画面共有しながら使い、参加者名簿からレース演出で当選者1名を決定する。

- 作成日: 2026-06-12
- ステータス: ドラフト(v1スコープ確定)

---

## 1. 目的・コンセプト

- 単なる「ガラガラポン」ではなく、**観戦して白熱できる抽選**を提供する
- 結果がわかるまでの数十秒に「抜きつ抜かれつ」「ゴール前接戦」などの射倖心をくすぐる展開を演出する
- 完全無料で公開・利用できる(サーバー費用ゼロ)

## 2. 利用シナリオ

1. 主催者がブラウザでツールのURLを開く
2. 参加者名を1行1名で貼り付け/入力する
3. テーマ(競馬・カーレース等)を選択する
4. Zoomで画面共有(「音声を共有」をON)
5. スタートボタンでレース開始 → 全員で観戦 → 1着が当選者
6. 必要なら当選者を除外して再レース(景品が複数ある場合)

- 操作するのは主催者のみ。参加者はZoomの共有画面で観戦する
- リアルタイム同期(参加者の各端末で観戦)はv1スコープ外

## 3. 機能要件

### 3.1 参加者登録

| 項目 | 内容 |
|---|---|
| 入力方式 | テキストエリアに1行1名。Excel/スプレッドシートからの貼り付けを想定 |
| 整形 | 空行・前後空白は自動除去。重複名は警告表示(そのまま実行も可) |
| 人数 | 2名〜100名程度を想定。テーマごとの同時表示上限を超える場合は予選/組分け表示で対応(後述) |
| 保存 | localStorageに自動保存し、リロード後も名簿が残る |

### 3.2 テーマ選択(v1)

| テーマ | 演出タイプ | ビジュアル方針 |
|---|---|---|
| 競馬 | レース型 | ドット絵レトロ(ファミコン競馬風)。実況テロップ・ファンファーレ |
| カーレース | レース型 | ドット絵 or フラット2D。エンジン音・ピットイン・スリップで順位変動 |
| アヒルボート | レース型 | フラット2D・コミカル。水流で流される、わき見をするなどゆるい演出 |
| カジノルーレット | 回転抽選型 | リッチなCSS/Canvas表現。回転→減速→隣で止まりそうで止まらないタメ演出 |

- ビジュアルはテーマごとに最適なスタイルを採用する(統一しない)
- v2候補テーマ: マラソン、バイク、自転車、競艇 等

### 3.3 抽選ロジック(公平性)

- 当選者は **レース開始時に `crypto.getRandomValues` で先に決定**し、演出は結果から逆算して生成する
- 演出中の順位変動はすべてスクリプト化された見せ方であり、結果には影響しない
- 各参加者の当選確率は完全に均等(重み付けなし)
- 不正疑念対策として「抽選方式について」の説明をヘルプに記載する

### 3.4 演出要件(レース型共通)

レースは以下の3幕構成とし、合計 **30〜60秒**(設定で短縮可)。

1. **導入(〜10秒)**: 出走表表示 → ファンファーレ → スタート
2. **中盤(15〜30秒)**: 順位の入れ替わりを複数回発生させる。最下位からのまくり、先頭の失速などのイベントをランダムに見せる(結果は不変)
3. **終盤(10〜20秒)**: ゴール前で当選者を含む2〜3名の接戦を必ず演出。僅差なら「写真判定」演出を挟んでから結果発表

結果発表:

- 1着(当選者)の名前を紙吹雪・ファンファーレとともに大きく表示
- ゴール直前のスロー再生(リプレイ)機能
- 「もう一度(当選者を除外して再レース)」ボタン

ルーレット型(カジノ):

- ポケットに参加者名を割り当て、ボールが減速 → 当選者の隣を行き来してから着地するタメ演出
- 結果発表まわりはレース型と共通

### 3.5 大人数時の扱い

- 1画面に表示できる頭数の上限はテーマごとに定義(競馬: 12、カーレース: 8、アヒル: 10、ルーレット: 24 を目安)
- 上限を超える場合は自動で組分けし、各組の1着で「決勝戦」を行う(トーナメント演出として盛り上げに転用)

### 3.6 音響(ハイブリッド方式)

| 種別 | 方式 | 備考 |
|---|---|---|
| BGM | フリー音源を同梱 | テーマ切替で自動的に曲も切替。利用規約の確認とクレジット表記をCREDITS.mdで管理 |
| 効果音 | Web Audio APIで合成 | ファンファーレ・歓声・蹄/エンジン音・ルーレットのカチカチ音など。音源ファイル不要 |

- ミュートボタン・音量スライダーを常時表示
- ブラウザの自動再生制限のため、音声はユーザー操作(スタートボタン)を起点に開始する
- 初回利用時にZoomの「サウンド共有」をONにする案内を表示する

### 3.7 抽選履歴

- セッション内の抽選結果(日時・テーマ・当選者)を一覧表示
- localStorageに保存し、クリアボタンで消去可能

## 4. 非機能要件

| 項目 | 要件 |
|---|---|
| ホスティング | GitHub Pages(無料)。ビルド成果物は静的ファイルのみ、サーバー・DB不要 |
| 対応ブラウザ | Chrome / Edge / Safari / Firefox の最新版(PC前提。主催者ツールのためスマホ対応は努力目標) |
| 画面 | 16:9のフルスクリーン表示に最適化(Zoom画面共有を想定)。F11/全画面ボタンあり |
| パフォーマンス | 60fps目標。Canvasベースで100名規模でも破綻しないこと |
| プライバシー | 名簿データは外部送信しない(完全クライアント完結)。その旨を画面に明記 |
| 多言語 | v1は日本語のみ |

## 5. 技術構成(案)

- **ビルド**: Vite + TypeScript
- **描画**: Canvas 2D(ドット絵テーマ)+ CSS/SVGアニメーション(ルーレット等)。v1ではゲームエンジン(PixiJS等)は使わず軽量に保つ
- **音響**: Web Audio API(効果音合成)+ `<audio>`(BGM再生)
- **状態管理**: フレームワークなし or 軽量(Preact等)。UI画面が少ないため素のTSでも可
- **テーマ拡張性**: 「テーマ = 演出モジュール」のインターフェースを定義し、テーマ追加がモジュール追加だけで済む設計にする
- **CI/CD**: GitHub Actions で main へのpush時に GitHub Pages へ自動デプロイ

## 6. 画面構成

1. **セットアップ画面**: 名簿入力 / テーマ選択(プレビュー付き) / レース時間設定(短い・普通・長い) / 音量
2. **レース画面**: フルスクリーン演出 + 実況テロップ + 順位表示
3. **結果画面**: 当選者発表 / リプレイ / 再レース(当選者除外) / セットアップに戻る
4. **履歴パネル**: 結果画面・セットアップ画面から開ける

## 7. マイルストーン

| フェーズ | 内容 |
|---|---|
| M1 | 基盤: セットアップ画面、抽選ロジック、テーマモジュールIF、競馬テーマ(演出+音) |
| M2 | カーレース・アヒルボートテーマ追加、組分け(決勝戦)対応 |
| M3 | カジノルーレット、履歴、リプレイ、仕上げ(全画面・案内表示) |
| M4 | GitHub Pages公開、README・クレジット整備 |
| v2候補 | 追加テーマ(マラソン・バイク・自転車)、複数当選(上位N名)、カスタムテーマ色、英語対応 |

## 8. リスク・留意点

- **音声共有の周知**: Zoomで「音声を共有」を忘れるとBGMが参加者に聞こえない → 画面内に常設の案内を出す
- **自動再生制限**: ブラウザ仕様上、操作なしで音は鳴らせない → スタート操作起点で問題なし
- **フリー音源の規約**: 商用利用可・クレジット表記条件を採用前に必ず確認し、CREDITS.mdに記録する
- **「ヤラセ」疑念**: 演出上の接戦が恣意的に見える可能性 → 抽選方式(開始時に乱数で確定)の説明をヘルプに明記
